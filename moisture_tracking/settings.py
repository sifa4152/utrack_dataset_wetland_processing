# -*- coding: utf-8 -*-

import os

WRKDIR = os.path.join() 

# PATH for output of moisture footprints (only folder location)
PATH_MOISTURE_FOOTPRINTS = os.path.join()
OUT_ESHEDS = os.path.join(PATH_MOISTURE_FOOTPRINTS,'forward') 
OUT_PSHEDS = os.path.join(PATH_MOISTURE_FOOTPRINTS,'backward')

# PATH to ERA5 data
PATH_DATA_ERA5_E = os.path.join() # evaporation
PATH_DATA_ERA5_P = os.path.join() # precipitation

# PATH to target cells
PATH_TARGET_ZONES = os.path.join()

# PATH to Utrack dataset (only folder location)
PATH_UTRACK = os.path.join()

# PATH to parameters (job distribution into [job, start, end])
PARAMS = os.path.join() 

